export enum Layouts {
  Dashboard = 'dashboard',
}

export enum Middlewares {
  Protected = 'protected',
}

export enum UserStatus {
  Active = 'active',
  Inactive = 'inactive',
  Locked = 'locked',
}